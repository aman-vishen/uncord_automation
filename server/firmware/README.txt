ETE Solutions India - Central Firmware Repository

Do not manually commit production firmware images to GitHub.
Use the Central Server GUI -> Select Firmware File.
The selected image is copied into this folder and its SHA-256 is stored in mac_server.db.
The Firmware ON/OFF switch is also persisted in mac_server.db.
