# ============================================================
# ETE / UNCORD MAC WRITER COMMANDS - v13.69
# FLOW:
# Firmware -> verify firmware -> factorymode set 1 -> firstboot -y -r
# -> wait for Telnet -> reserve identity -> write -> verify
# ============================================================

[WRITE]
prolinecmd macaddr set {MAC_NOSEP}
prolinecmd hwsn set {SERIAL}
prolinecmd gponsn set {GPON}
prolinecmd telnetEnable set 1

[VERIFY]
prolinecmd macaddr get
prolinecmd hwsn get
prolinecmd gponsn get

[FINALIZE]
# Intentionally empty in v13.69.
