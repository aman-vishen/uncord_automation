let state={recent:[]};
const $=s=>document.querySelector(s); const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function params(){return `start=${$('#start').value}&end=${$('#end').value}`}
function badge(v){v=v||'-';return `<span class="badge ${esc(v)}">${esc(v)}</span>`}
async function load(){try{let r=await fetch('/api/dashboard?'+params());if(!r.ok)throw new Error('HTTP '+r.status);let d=await r.json();state=d;render(d);$('#healthDot').style.background='#1f9d62';$('#healthText').textContent='MES online'}catch(e){$('#healthDot').style.background='#d84949';$('#healthText').textContent='MES offline'}}
function render(d){const k=d.kpi;let cards=[['Line Input',k.line_input,'Wi-Fi Calibration tested'],['Finished Output',k.production_volume,'Verification tested'],['Final PASS',k.final_pass,`${k.final_yield}% final yield`],['Final FAIL',k.final_fail,'FAIL + ERROR'],['Estimated WIP',k.wip,'Input minus output'],['Available MACs',k.available_macs,'Central MAC pool']];$('#kpis').innerHTML=cards.map(x=>`<div class="kpi"><div class="label">${x[0]}</div><div class="value">${x[1]}</div><div class="sub">${x[2]}</div></div>`).join('');$('#rangeText').textContent=`${d.range.start} to ${d.range.end}`;renderProcess(d.stages);renderDaily(d.daily);renderYield(k.final_yield,k.final_pass,k.final_fail);renderStages(d.stages);renderStations(d.stations);renderRecords(d.recent);$('#export').href='/export/production.csv?'+params()}
function renderProcess(rows){$('#processFlow').innerHTML=rows.map((r,i)=>`<div class="process-step"><div class="step-no">${i+1}</div><div class="step-name">${esc(r.stage.replace(/^\d+\.\s*/,''))}</div><div class="step-total">${r.tested}</div><div class="step-results"><span class="pass-text">${r.pass} PASS</span><span class="fail-text">${r.fail} FAIL</span></div><div class="track"><div class="fill" style="width:${r.yield}%"></div></div><strong>${r.yield}%</strong></div>${i<rows.length-1?'<div class="process-arrow">›</div>':''}`).join('')}
function renderDaily(rows){let max=Math.max(1,...rows.map(r=>r.total));$('#dailyChart').innerHTML=rows.length?rows.map(r=>`<div class="day"><div class="bar pass" title="PASS ${r.pass}" style="height:${r.pass/max*100}%"></div><div class="bar fail" title="FAIL ${r.fail}" style="height:${r.fail/max*100}%"></div><label>${r.day.slice(5)}</label></div>`).join(''):'<div class="muted">No finished production data in this date range.</div>'}
function renderYield(y,p,f){$('#yieldRing').innerHTML=`<div class="ring" style="background:conic-gradient(#1e84c6 ${y}%,#e5edf3 0)"><div class="inside"><strong>${y}%</strong><small>${p} PASS / ${f} FAIL</small></div></div>`}
function renderStages(rows){$('#stageOverview').innerHTML=rows.map(r=>`<div class="stage-row"><span>${esc(r.stage)}</span><div class="track"><div class="fill" style="width:${r.yield}%"></div></div><strong>${r.yield}%</strong></div>`).join('');$('#stageTable').innerHTML=rows.map(r=>`<tr><td>${esc(r.stage)}</td><td>${r.tested}</td><td>${r.pass}</td><td>${r.fail}</td><td><strong>${r.yield}%</strong></td><td><div class="track"><div class="fill" style="width:${r.yield}%"></div></div></td></tr>`).join('')}
function renderStations(rows){$('#stationTable').innerHTML=rows.length?rows.map(r=>`<tr><td>${esc(r.client_id)}</td><td>${r.total}</td><td>${r.pass}</td><td>${r.fail}</td><td><strong>${r.yield}%</strong></td><td>${esc(r.last_result||'-')}</td></tr>`).join(''):'<tr><td colspan="6" class="muted">No station data.</td></tr>'}
function renderRecords(rows){state.recent=rows;filterRecords()}
function shortFile(v){if(!v)return '-';return String(v).split(/[\\/]/).pop()}
function filterRecords(){let q=$('#search').value.toLowerCase();let rows=(state.recent||[]).filter(r=>Object.values(r).join(' ').toLowerCase().includes(q));$('#recordsTable').innerHTML=rows.length?rows.map(r=>`<tr><td>${esc(r.completed_at||'-')}</td><td>${esc(r.stage||'-')}</td><td>${esc(r.mac||'-')}</td><td>${esc(r.serial_number||'-')}</td><td>${esc(r.gpon_number||'-')}</td><td>${esc(r.pcb_serial_number||'-')}</td><td>${esc(r.part_number||'-')}</td><td>${esc(r.client_id||'-')}</td><td>${esc(r.router_ip||'-')}</td><td title="${esc(r.source_file||'')}">${esc(shortFile(r.source_file))}</td><td>${badge(r.status)}</td><td class="detail-cell" title="${esc(r.detail||'')}">${esc((r.detail||'-').slice(0,100))}</td></tr>`).join(''):'<tr><td colspan="12" class="muted">No records in this date range.</td></tr>'}
document.querySelectorAll('nav button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('nav button,.view').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.view).classList.add('active')}));$('#apply').addEventListener('click',load);$('#search').addEventListener('input',filterRecords);load();setInterval(load,Math.max(5,window.MES_REFRESH||10)*1000);


async function traceSearch(){let q=$('#traceQuery').value.trim();if(!q)return;$('#traceTable').innerHTML='<tr><td colspan="9" class="muted">Searching…</td></tr>';try{let r=await fetch('/api/traceability?q='+encodeURIComponent(q));let d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||('HTTP '+r.status));if(!d.found){$('#traceIdentity').innerHTML='';$('#traceTable').innerHTML='<tr><td colspan="9" class="muted">No matching production identity found.</td></tr>';return}let i=d.identity||{};$('#traceIdentity').innerHTML=[['MAC',i.mac],['Serial',i.serial_number],['GPON',i.gpon_number],['PCB Serial',i.pcb_serial_number]].map(x=>`<div class="kpi"><div class="label">${esc(x[0])}</div><div class="value" style="font-size:18px">${esc(x[1]||'-')}</div></div>`).join('');$('#traceTable').innerHTML=(d.history||[]).map(x=>`<tr><td>${esc(x.completed_at||'-')}</td><td>${esc(x.stage||'-')}</td><td>${esc(x.mac||'-')}</td><td>${esc(x.serial_number||'-')}</td><td>${esc(x.gpon_number||'-')}</td><td>${esc(x.pcb_serial_number||'-')}</td><td>${esc(x.station_id||'-')}</td><td>${badge(x.status)}</td><td class="detail-cell">${esc(x.detail||'-')}</td></tr>`).join('')||'<tr><td colspan="9" class="muted">No history found.</td></tr>'}catch(e){$('#traceTable').innerHTML=`<tr><td colspan="9" class="muted">${esc(e.message)}</td></tr>`}}
$('#traceSearch').addEventListener('click',traceSearch);$('#traceQuery').addEventListener('keydown',e=>{if(e.key==='Enter')traceSearch()});
